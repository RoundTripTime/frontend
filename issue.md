## 현재 이슈

- Node/npm 버전 불일치
  - `package.json` engines: Node `>=20.19.0 <21`, npm `>=10.0.0 <11`
  - 현재 작업 환경: Node `v24.4.1`, npm `11.4.2`
  - npm install 시 `EBADENGINE` 경고가 발생한다.
  - 실제 개발/CI 기준은 `.nvmrc`의 Node `20.19.4`로 맞춘다.

- 화면 명세 - s-05가 장소 수락/거절 기능까지 하고 있는데, 이 기능은 추출한 장소를 플레이스에 넣을지 말지에 결정하는 로직이고 이미 결정된 장소를 관리하는 기능은 아님 서로 별도의 화면으로 만들어야할 듯
